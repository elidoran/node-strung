{
  "name": "strung",
  "version": "1.2.1",
  "description": "Acts as a stream to send a string, gather a string, or both.",
  "main": "lib/index.js",
  "files": [
    "lib/index.js"
  ],
  "keywords": [
    "string to stream",
    "string stream",
    "stream to string",
    "stream string",
    "stream from string",
    "string from stream",
    "convert",
    "string",
    "stream",
    "source",
    "sink",
    "pipe",
    "piping",
    "readable",
    "writable",
    "duplex",
    "io",
    "input",
    "output",
    "memory"
  ],
  "author": "Eli Doran <eli+npm@elidoran.com>",
  "license": "MIT",
  "scripts": {
    "lint": "eslint --no-eslintrc -c build/eslint/eslint.js --cache --cache-location build/eslint/cache lib/index.js",
    "test": "mocha --R spec --bail --check-leaks test/lib/*.js",
    "test-older": "nvs --versions 10 npm test",
    "test-stable": "nvs --versions 12 npm test",
    "test-latest": "nvs --versions 14 npm test",
    "tests": "nvs --versions 10,12,14 npm run test",
    "cover": "nyc --silent --no-clean npm run test",
    "cover-older": "nvs --versions 10 npm run cover",
    "cover-stable": "nvs --versions 12 npm run cover",
    "cover-latest": "nvs --versions 14 npm run cover",
    "covers": "nvs --versions 10,12,14 npm run cover",
    "coverage": "npm run covers && nyc report --reporter=lcov --reporter=text-summary",
    "travis": "npm run covers && nyc report --reporter=text-lcov | coveralls",
    "browser": "npm run cover && nyc report --reporter=html"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/elidoran/node-strung.git"
  },
  "bugs": "http://github.com/elidoran/node-strung/issues",
  "devDependencies": {
    "coffee-script": "^1.12.7",
    "coveralls": "^3.1.0",
    "eslint": "^7.0.0",
    "mocha": "^7.1.2",
    "mocha-lcov-reporter": "^1.3.0",
    "nave": "^3.2.1",
    "nvs": "^0.3.0",
    "nyc": "^15.0.1",
    "through2": "^3.0.1"
  },
  "dependencies": {},
  "engines": {
    "node": ">=6"
  }
}
